{
  "name": "incidentrecorder",
  "version": "2.8.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check deepgram.js && node --check cloudflare/worker.mjs && node --check src/main.js && npm test"
  }
}
